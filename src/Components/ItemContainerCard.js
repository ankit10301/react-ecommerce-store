import styled from "styled-components";

const ItemContainerCard = styled.div`
    &: hover{
        box-shadow: 0 3px 15px rgba(128, 128, 128, 0.4);
        border-radius: 0.375rem;
    }
`;

export default ItemContainerCard;